[package]
name = "spnd"
# The release workflow injects the tagged version before publishing.
version = "0.0.0"
edition = "2021"
description = "Interactive terminal UI for coding-agent token usage and spend"
license = "MIT"
repository = "https://github.com/Royalflamejlh/spnd"
homepage = "https://github.com/Royalflamejlh/spnd"
readme = "README.md"

# Standalone crate: the real application is the `spnd` package inside the
# rust/ workspace, which cannot be published because of its internal path
# dependencies. This crate keeps the crates.io name pointing at the real
# installation channels.
[workspace]
